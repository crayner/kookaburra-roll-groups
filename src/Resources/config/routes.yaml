controllers:
    resource: '@KookaburraRollGroupsBundle/Controller/'
    type: annotation
    schemes: [ 'https' ]
    # this is added to the beginning of all imported route URLs
    prefix: '/roll/groups'
    # this is added to the beginning of all imported route names
    name_prefix: 'roll_groups__'
